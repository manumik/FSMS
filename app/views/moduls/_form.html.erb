<div style="margin:0 auto;">
    <div class="card border-dark mb-3" style="max-width: 100%;margin: 5px;">
      <div class="card-header">Skripten</div>
      <div class="card-body text-dark">


<%= form_with(model: modul, local: true) do |form| %>
  <% if modul.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(modul.errors.count, "error") %> prohibited this modul from being saved:</h2>

      <ul>
      <% modul.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name, id: :modul_name %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>

</div>
</div>
</div>